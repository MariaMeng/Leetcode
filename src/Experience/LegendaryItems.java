package Experience;
import java.util.*;
/**
 * Created by apple on 17/3/31.
 */
public class LegendaryItems {
    class TreeNode {
        int count; // store the number of item
        double value; // get the prob
        boolean isItem;
        int flag; // num of legend

    }
    TreeNode root;
    public LegendaryItems() {
        root = new TreeNode();
    }
    /*
    public void insert(int P, int Q, int N) {
        TreeNode node = root;
        if (node.count == 0 && if ())
    }
    */
}
